# Base URL of the Little World API (apps/api).
# Local dev: the API runs on port 3000.
NEXT_PUBLIC_API_URL=http://localhost:3000

# Name of the httpOnly cookie that stores the admin session JWT.
SESSION_COOKIE_NAME=lw_session
